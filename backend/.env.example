# CORTEX Backend Configuration
#
# SETUP INSTRUCTIONS FOR NEW CONTRIBUTORS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace placeholder values below with real API keys
# 3. Never commit .env to git (already protected by .gitignore)
#
# To get API keys:
# - OpenAI API Key: https://platform.openai.com/api-keys

# Database Configuration
DATABASE_URL=sqlite:///./cortex.db

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# OpenAI Vector Store Configuration
# Leave blank to auto-create on first ingest
OPENAI_VECTOR_STORE_ID=

# UMAP Configuration
UMAP_N_NEIGHBORS=15
UMAP_MIN_DIST=0.1

# Clustering Configuration
N_CLUSTERS=5

# Server Configuration
HOST=0.0.0.0
PORT=8000
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Environment
ENVIRONMENT=development