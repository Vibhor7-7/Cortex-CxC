# =============================================================================
# CORTEX Environment Variables
# =============================================================================

# --- Provider Selection (auto-detects from API keys if not set) ---
# EMBEDDING_PROVIDER=huggingface   # "huggingface" (cloud) or "ollama" (local)
# CHAT_PROVIDER=groq               # "groq" (cloud) or "ollama" (local)

# --- Cloud API Keys (required for cloud providers) ---
# HF_API_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Ollama (local, default) ---
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=qwen2.5
# OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# --- Groq model override ---
# GROQ_MODEL=llama-3.1-8b-instant

# --- Database ---
# DATABASE_URL=sqlite:///cortex.db

# --- Storage paths (for Docker / Render persistent disk) ---
# VECTOR_STORE_PATH=/data/.vector_store.json
# MODEL_DIR=/data/.models
# CACHE_DIR=/data/.cache

# --- Server ---
# HOST=0.0.0.0
# PORT=8000

# --- CORS (comma-separated origins, or * for all) ---
# CORS_ORIGINS=https://your-frontend.vercel.app,http://localhost:5500
